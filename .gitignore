node_modules
dist
.idea/
*.bak
test/perf/generated.ts
test/perf/tsconfig.json

# `npm pack` extracted here while verifying the published tarball. Never tracked,
# but ESLint walks into it and chokes on files outside the tsconfig project.
package/

# Lavish review surfaces — scratch HTML for reviewing plans, not project content.
.lavish/

# Superpowers working files — agent skill state and implementation plans, not
# project content. Never track these.
.superpowers/
docs/superpowers/
