node_modules
dist
.idea/
*.bak
test/perf/generated.ts
test/perf/tsconfig.json

# `npm pack` extracted here while verifying the published tarball. Never tracked,
# but ESLint walks into it and chokes on files outside the tsconfig project.
package/
