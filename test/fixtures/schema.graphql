type Query {
  posts: [Post!]!
  post(id: ID!): Post
  drafts: [Post!]
}

type Mutation {
  createUser(name: String!, email: String!, age: Int): User!
}

type Post {
  id: ID!
  title: String!
  author: User!
}

type User {
  id: ID!
  firstName: String!
  lastName: String
}
