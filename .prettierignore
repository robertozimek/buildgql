dist
node_modules
test/perf/generated.ts
test/perf/tsconfig.json
package-lock.json
# Agent scratch (SDD ledgers, task briefs/reports, review diffs) — not project source.
# Prettier's default --ignore-path is [.gitignore, .prettierignore] (root .gitignore only;
# it does not descend into nested .gitignore files). What actually excludes this directory
# from git is the nested .superpowers/sdd/.gitignore ("*"), which Prettier never reads —
# hence the separate entry here.
.superpowers
